// Window for adding a viewing a job which is currently *in memory*. Need to add special case for database access
// Matt Rossouw (omeh-a)
// 08/22

import React from 'react';
import styled from 'styled-components';

const ViewJob: React.FC = () => {
    return(
        <div>
            <h1>Job viewer</h1>
        </div>
    )
}

export default ViewJob;