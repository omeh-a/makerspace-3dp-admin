// Window for adding a new job from desk service. This also acts as the input for SharePoint jobs until that process is automated.
// Matt Rossouw (omeh-a)
// 08/22

import React from 'react';
import styled from 'styled-components';

const Deskservice: React.FC = () => {
    return(
        <div>
            <h1>Desk service</h1>
        </div>
    )
}

export default Deskservice;